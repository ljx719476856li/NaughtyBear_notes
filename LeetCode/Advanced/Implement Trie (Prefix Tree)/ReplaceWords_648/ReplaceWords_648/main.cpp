#include "Trie.h"


void main()
{
	Solution sln;

	vector<string> dic = {"desk", "cat", "candy", "bat", "rat" };
	string str = sln.replaceWords(dic, "the cattle was rattled by the battery");


	return;
}